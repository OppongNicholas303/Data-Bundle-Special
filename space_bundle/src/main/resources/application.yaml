spring:
  application:
    name: space-bundle

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:465}
    username: ${MAIL_USERNAME:nicholas.oppong@stu.ucc.edu.gh}
    password: ${MAIL_PASSWORD:youdkvfjtbxvstgp}
    properties:
      mail:
        debug: false
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false

  # MongoDB Configuration
  data:
    mongodb:
      uri: "mongodb+srv://nicholasoppong2623_db_user:Yaw0243911336@cluster0.arawb0h.mongodb.net/bundle?retryWrites=true&w=majority&connectTimeoutMS=10000&socketTimeoutMS=60000&serverSelectionTimeoutMS=10000&maxIdleTimeMS=45000&heartbeatFrequencyMS=10000"
      auto-index-creation: true
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.mongo.embedded.EmbeddedMongoAutoConfiguration

  # Disable default security password
  security:
    user:
      name: disabled
      password: disabled

# JWT Configuration
jwt:
  secret: default-jwt-secret-key-change-in-production-minimum-256-bits-required
  expiration: ${JWT_EXPIRATION:1800000} # 30 minutes in milliseconds
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000} # 7 days in milliseconds
  issuer: space-bundle
  header: Authorization
  prefix: "Bearer "

# Server Configuration
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: ${INCLUDE_STACKTRACE:never}
    include-exception: false
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
  http2:
    enabled: true
  shutdown: graceful

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
    enabled-by-default: false
  endpoint:
    health:
      enabled: true
      show-details: ${HEALTH_SHOW_DETAILS:when-authorized}
      show-components: ${HEALTH_SHOW_COMPONENTS:when-authorized}
      probes:
        enabled: true
    info:
      enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
    mongo:
      enabled: true

# Application Info
info:
  app:
    name: ${spring.application.name}
    description: Space Bundle Application
    version: 0.0.1-SNAPSHOT
    encoding: UTF-8
  java:
    version: 21

# Logging — managed by logback-spring.xml
logging:
  file:
    name: ${LOG_FILE:logs/space-bundle.log}
    max-size: ${LOG_MAX_SIZE:10MB}
    max-history: ${LOG_MAX_HISTORY:30}
    total-size-cap: ${LOG_TOTAL_SIZE:1GB}

# Moolre configuration
moolre:
  sms:
    api-key: ${MOOLRE_SMS_API_KEY:YOUR_X_API_VASKEY}
    api-url: ${MOOLRE_SMS_API_URL:https://api.moolre.com/open/sms/send}
    price-per-page: ${MOOLRE_SMS_PRICE_PER_PAGE:0.04}
    sender-ids: ${MOOLRE_SMS_SENDER_IDS:Moolre,SpaceBundle}
  api-user: ${MOOLRE_API_USER:adamssaeed}
  pub-key: ${MOOLRE_PUB_KEY:eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VyaWQiOjEwNzkzMCwiZXhwIjoxOTU2NTQ1OTk5fQ.S7lP17aOqbyZETCUWITrn4E0chXiJ2aReg1a-9zRr8E}
  private-key: ${MOOLRE_PRIVATE_KEY:72tszSwQwjC6qwYHRLSFhlKjHZlDN38x2ou7RXFn8e6RBRESI9vW7Ag1d5p2HuFt}
  account-number: ${MOOLRE_ACCOUNT_NUMBER:10793006063230}
  webhook-secret: ${MOOLRE_WEBHOOK_SECRET:7150c99a-e388-47b4-8358-c2661422c2c3}
  callback-url: ${MOOLRE_CALLBACK_URL:https://data-bundle-special-ulkt.onrender.com/api/webhook/moolre}
  redirect-url: ${MOOLRE_REDIRECT_URL:http://localhost:8081/orders}

# Bot Configuration
bot:
  api:
    url: https://mydatagigs.com/wp-json/custom/v1/place-order
    urlRandy: https://myspaceserver.com/api
    token: tera_live_d7a798a4790c08e51366d0580276835b
    tokenRandy: sk_4975646ef9cc4a5a2bcfd62c0f60f8a0a713de8114df7c47965b2703a9d555f7
    timeout: 30000
    login:
      url: https://mydatagigs.com/login/
      username: nicholas.oppong2623@gmail.com
      password: Vida0243911336@

mashup:
  offers:
    url: https://myspaceserver.com/api/external/special-offers/mashup/orders
    timeout-seconds: ${MASHUP_OFFERS_TIMEOUT_SECONDS:8}

# Frontend Configuration
app:
  frontend-url: http://localhost:8081
  support-email: support@tapdata.com
  storefront-base-url: https://tapdata-agent-store.vercel.app/store

# CheckerPort Configuration
checkerport:
  base-url: ${CHECKERPORT_BASE_URL:https://api.checkerport.com}
  api-key: ${CHECKERPORT_API_KEY:r_qiGMfyzK02d-uCjJJXrL1Xzt9pSenP-Nv8w9ZVWMw}
  webhook-callback-url: ${CHECKERPORT_WEBHOOK_URL:https://data-bundle-special-ulkt.onrender.com/api/webhook/checkerport}

---
# Development Profile
spring:
  config:
    activate:
      on-profile: dev

  data:
    mongodb:
      uri: mongodb://localhost:27017/space_bundle_dev

jwt:
  secret: dev-secret-key-change-in-production-minimum-256-bits-required-for-hs256
  expiration: 86400000
  refresh-expiration: 604800000

server:
  error:
    include-stacktrace: on_param

management:
  endpoint:
    health:
      show-details: always
      show-components: always

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.space: ${LOG_LEVEL_APP:INFO}
    org.mongodb.driver.cluster: WARN
